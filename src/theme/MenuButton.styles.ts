import {StyleSheet} from 'react-native';

export const menuButtonStyles = StyleSheet.create({
  button: {padding: 20},
  buttonInfoContainer: {flexDirection: 'row'},
  buttonText: {flex: 1, fontSize: 20, fontWeight: 'bold'},
  buttonIcon: {fontSize: 20, fontWeight: 'bold'},
});
